---
import PostOverview from "./PostOverview.astro";

const posts = await Astro.glob("/posts/*.md");
---

<div class="row">
  <h3 class="section__title">Ultimele articole</h3>
  <ul class="articles-list">
    <!-- {
      posts.map((post) => (
        <PostOverview
          date={post.frontmatter.date}
          image={post.frontmatter.slug}
          slug={post.frontmatter.slug}
          title={post.frontmatter.title}
          excrept={post.frontmatter.excrept}
          author={post.frontmatter.author}
        />
      ))
    } -->
    <p>Niciun articol deocamdată!</p>
  </ul>
</div>

<style>
  .row {
    padding: 1rem 2rem;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  p {
    text-align: center;
    font-size: 5rem;
  }

  .articles-list {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: 2rem;
  }

  @media (max-width: 1600px) {
    .articles-list {
      grid-template-columns: 1fr 1fr;
    }
  }

  @media (max-width: 1100px) {
    .articles-list {
      grid-template-columns: 1fr;
    }
  }
</style>
